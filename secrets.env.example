# Samba Configuration
# SAMBA_PASSWORD=your_samba_password

# Restic backup repository (auto-filled by bootstrap/install.sh; edit to change)
# RESTIC_REPO=/srv/backup/homelab

# Restic backup encryption password (required by scripts/backup.sh)
# RESTIC_PASSWORD=your_restic_backup_password
