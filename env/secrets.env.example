# No-IP DDNS Configuration
# The ddns-updater container uses a config.json file in /srv/data/noip/
# After first run, configure via the web UI at http://your-server:8000
# Or create /srv/data/noip/config.json manually with your DDNS settings
# See: https://github.com/qdm12/ddns-updater for config format

# Add other secrets here as needed
# Example: API keys, database passwords, etc.

